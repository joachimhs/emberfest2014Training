<p>Selected Album: {{id}}</p>

<div class="row">
    {{#each photo in photos}}
        <div class="col-md-2">
            {{#link-to "album.photo" photo}}
                <img {{bind-attr src=photo.thumbnail}} />
            {{/link-to}}
        </div>
    {{/each}}

    {{outlet}}
</div>